#!/bin/sh
# Notebook checks moved to CI (.github/workflows/ci.yml's `notebooks` job),
# gated to run only on tag push before `publish` -- not on every local commit.
# Run manually any time with: python scripts/run_notebooks.py
